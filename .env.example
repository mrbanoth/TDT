# Razorpay Configuration (Test Mode)
VITE_RAZORPAY_KEY=rzp_test_YOUR_ACTUAL_KEY_ID
RAZORPAY_KEY_ID=rzp_test_YOUR_ACTUAL_KEY_ID
RAZORPAY_SECRET=YOUR_ACTUAL_KEY_SECRET

# Application URLs
VITE_APP_URL=http://localhost:8080
PORT=3001

# Environment
NODE_ENV=development

# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
ADMIN_EMAIL=your-email@gmail.com

# Email TLS Configuration (for Windows development)
SMTP_ALLOW_INSECURE_TLS=false

# Instructions:
# 1. Copy this file to .env (don't commit .env to git)
# 2. Replace the X's with your actual Razorpay test keys
# 3. Get test keys from: https://dashboard.razorpay.com/test/keys
# 4. For production, replace test keys with live keys
# 5. Set up Gmail App Password for SMTP
# 6. Set SMTP_ALLOW_INSECURE_TLS=true if you have TLS issues on Windows
# 7. Never commit real keys to version control
